CREATE DEFINER=`root`@`localhost` PROCEDURE `CreateAdmin`(IN inID INT ,IN inName VARCHAR(45), IN inPassword VARCHAR(45))
BEGIN
	INSERT INTO Admin(idAdmin,Name,Password) VALUES (inID,inName,inPassword); 
       

END


/
/
/
CREATE DEFINER=`root`@`localhost` PROCEDURE `CreateCustomer`(IN inID INT, IN inName VARCHAR(45), IN inPassword VARCHAR(45))
BEGIN

INSERT INTO Customer(idCustomer,Name,Password) VALUES (inID,inName,inPassword);

END


/
/
/

CREATE DEFINER=`root`@`localhost` PROCEDURE `CreateOrderhistory`(IN inID INT, IN inPrice DOUBLE,IN inCustomerID INT, IN RestaurantID INT)
BEGIN

INSERT INTO OrderHistory(idOrderHistory,Price,Date,Customer_idCustomer,Restaurant_idRestaurant) VALUES (inID,inPrice,current_date(),inCustomerID,RestaurantID);

END

/
/
/


CREATE DEFINER=`root`@`localhost` PROCEDURE `CreateRestaurant`(IN inID INT,IN inName VARCHAR(45), IN inFoodType VARCHAR(45), IN inPrice INT)
BEGIN

INSERT INTO restaurant(idRestaurant,Name,FoodType,Price) VALUES (inID,inName,inFoodType,inPrice);



END

